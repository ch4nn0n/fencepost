#!/usr/bin/env bash
# No-op statusline for demo/fencepost.tape recordings. Outputs nothing, so
# the recording never shows whatever statusLine happens to be configured
# globally on the machine doing the recording (usage %, plan/rate-limit
# details, etc.) instead of Claude Code's own chrome.
exit 0
